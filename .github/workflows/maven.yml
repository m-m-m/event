name: build

on: [push]

env:
  GITHUB_USERNAME: x-access-token
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Set up JDK 11
      uses: actions/setup-java@v1
      with:
        java-version: 11
    - name: Build with Maven
      run: mvn -B --settings .mvn/settings.xml deploy -Dregistry=https://maven.pkg.github.com/m-m-m -Dtoken=${GITHUB_TOKEN}
